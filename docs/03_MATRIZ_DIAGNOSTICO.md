# Fase I | Matriz de diagnóstico obligatoria

Completa al menos cinco filas con evidencias reales.

| # | Ubicación | Smell / problema | Categoría | Impacto | Candidato | Prueba necesaria |
|---:|---|---|---|---|---|---|
| 1 | `ServicioReservas.procesar()`, líneas 10-18 completas | Un único método de 9 líneas concentra 4 validaciones, el cálculo de tarifa, un `println` de persistencia y un `println` de notificación. No hay ningún método privado que separe estas fases. | Long Method / Long Class (clase con una sola razón de existir que en realidad tiene 5-6 razones de cambio, ver Fase D) | Cualquier cambio en una fase (p. ej. la regla VIP) obliga a releer y volver a probar todo el método, incluida la persistencia y la notificación, que son ajenas al cambio. Alto riesgo de "shotgun surgery" cuando crezcan las reglas de tarifa. | Extraer notificación y cálculo de tarifa a colaboradores dedicados (posterior a este laboratorio) | `reservaValidaSeConfirma()` — verificar que tras extraer, una entrada válida sigue quedando `CONFIRMADA` y retornando el mismo total que hoy |
| 2 | `ServicioReservas.procesar()`, línea `if (r.getCorreo() == null \|\| !r.getCorreo().contains("@"))` | `ServicioReservas` pregunta tres veces por los datos internos de `Reserva` (`getCorreo()`, `getInicio()`, `getFin()`, `getTipo()`) para tomar decisiones que son responsabilidad de esos propios datos (validar formato, validar periodo). | Feature Envy | La regla de "qué es un correo válido" vive fuera de la clase dueña del dato; si mañana `Reserva` cambia su representación de correo, hay que tocar `ServicioReservas` además de `Reserva`. | Introducir `Correo` como Value Object con su propia validación | `correoSinArrobaEsInvalido()` — un correo sin "@" debe seguir siendo rechazado (retorno 0, sin confirmar) |
| 3 | `Reserva` — campos `correo` (String) y `tipo` (String) | El correo se representa como `String` plano y el tipo de reserva ("NORMAL"/"VIP") también, en vez de un tipo propio o un enum. Cualquier cadena arbitraria es un `tipo` válido para el compilador. | Primitive Obsession | Nada impide construir una `Reserva` con `tipo="vip"` (minúsculas) o `tipo="Vip "` con espacio, que el `equals()` de `"VIP".equals(...)` rechazaría silenciosamente sin avisar del error de escritura — el descuento simplemente no se aplicaría, sin ningún mensaje. | Introducir `Correo` (Value Object) y `TipoReserva` (enum) | `tipoConEspaciosNoAplicaDescuentoHoy()` — caracterizar que hoy `" VIP"` o `"vip"` NO reciben el descuento (documentar el comportamiento actual, sea o no el deseado) |
| 4 | `Reserva` — campos `inicio` y `fin` (dos `LocalDateTime` independientes) | El periodo de la reserva se modela como dos fechas sueltas en vez de un concepto único ("periodo" o "rango"). La regla `fin > inicio` vive en `ServicioReservas`, no junto a los datos que valida. | Data Clump / Primitive Obsession | `inicio` y `fin` siempre viajan juntos y siempre se validan juntos; hoy es fácil construir una `Reserva` con `fin` anterior a `inicio` porque el constructor no lo impide — la inconsistencia solo se detecta (parcialmente) cuando se llama a `procesar()`, y solo devuelve `0`, no informa el motivo. | Introducir `PeriodoReserva` que garantice `fin > inicio` en su propia construcción | `periodoInvalidoNoProcesa()` — un `fin` igual a `inicio` debe seguir devolviendo 0 y dejando el estado en `PENDIENTE` |
| 5 | `ServicioReservas.procesar()`, las 4 sentencias `if (...) { return 0; }` seguidas | Cuatro validaciones independientes retornan el mismo valor centinela `0` sin distinguir la causa (correo inválido, periodo inválido, anticipación insuficiente o `r == null` son indistinguibles desde fuera). | Manejo de errores débil / retorno ambiguo (código "difícil de razonar", conditional logic sin expresar la regla) | Un consumidor de `procesar()` no puede saber por qué una reserva fue rechazada ni distinguir "rechazada" de "cobró 0 legítimamente" si en el futuro existiera una tarifa gratuita. Dificulta dar retroalimentación al usuario final. | Sustituir el retorno primitivo por un resultado explícito (tipo `ResultadoProcesamiento` o similar) — **fuera de alcance de este laboratorio**, pero debe quedar registrado como hallazgo | `motivoDeRechazoQuedaRegistrado()` — prueba de caracterización que documenta, para cada una de las 4 causas de rechazo, que hoy el retorno es idénticamente `0.0` (para poder comparar contra el comportamiento nuevo cuando se implemente) |
| 6 | `ServicioReservas.procesar()`, línea `horasAnticipacion < 2` (parámetro `int`) más el `Reserva` de 5 parámetros posicionales | El método recibe un `int horasAnticipacion` sin unidad explícita en el tipo (podría confundirse con minutos o días), y el constructor de `Reserva` recibe 5 parámetros posicionales del mismo "tipo genérico" (`String, String, LocalDateTime, LocalDateTime, String`), fáciles de invertir por error (p. ej. intercambiar `id` y `correo`) sin que el compilador lo detecte. | Long Parameter List / Primitive Obsession | Un error de orden de argumentos en la construcción de `Reserva` (dos `String` seguidos) compila sin advertencia y falla en tiempo de ejecución de forma silenciosa (p. ej. el "correo" termina siendo el id). El límite `< 2` además está embebido como número mágico sin nombre. | Agrupar los parámetros relacionados (Introduce Parameter Object) y nombrar la constante de anticipación mínima | `limiteDeDosHorasEsInclusivo()` — caracterizar que exactamente `2` horas se considera válido (frontera `>= 2`) y `1` no, para no invertir la comparación al refactorizar |

Evita diagnósticos genéricos como:

- "código feo"
- "mala práctica"
- "viola SOLID"

Debes indicar **dónde**, **qué ocurre**, **qué riesgo produce** y **cómo podrías verificar el cambio**.
